<h1>店舗詳細</h1>

<table>
  <tr>
    <th>店舗</th>
    <th>店舗住所</th>
    <th>店舗電話番号</th>
  </tr>
  <tr>
    <td><%= @store.name %></td>
    <td><%= @store.address %></td>
    <td><%= @store.phone %></td>
  </tr>
</table>

<%= link_to "編集", Rails.application.routes.url_helpers.edit_store_path(@store), class: "btn btn-warning" %>

<h3>従業員一覧</h3>
<!-- TODO:一覧を作る -->

<h3>従業員を招待する場合は,従業員のemailを下記フォームより送信してください</h3>
<div class="row">
  <div class="col-sm-4">
    <%= form_with url: Rails.application.routes.url_helpers.invitation_store_path(@store) do |f| %>
      <%= f.label :email %>
      <%= f.email_field :email, class: "form-control mb-2" %>
      <%= f.submit "invite", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>

<%= link_to "業者", store_vendors_path(@store), class: "btn btn-primary" %>

<%= link_to "投稿", store_posts_path(@store), class: "btn btn-primary" %>

  
